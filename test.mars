
#!infer

type MathFn = function(number) -> number

let factorial = function(n: number): number {
	if n == 1 { return 1 }
	return n * factorial(n - 1)
}

let apply = function(x: number, f: MathFn): number => f(x)

let main = function(): void {

	let x = apply(7, factorial)

	writeline(x)
}

main()
