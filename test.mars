
let div = function(a, b) {
	if b == 0 {
		return error 'cannot divide by zero'
	}

	return a / b
}

let foo = function(a, b) {
	return div(b, a)? + div(a, b)?
}

writeline(foo(2, 3))
writeline(foo(4, 0))
writeline(foo(3, -1))
writeline(foo(0, 0)?)
