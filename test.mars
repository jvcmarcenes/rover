
function factorial(n: number): number {
	if n <= 1 {
		return 1
	}
	return n * factorial(n - 1)
}

function main() {

	let x = 23
	let y = factorial('oi')

	writeline('#{x}! = #{factorial('oi')}')

}
